# laughing-memory

Telegram-бот на архитектуре **Orchestrator v2**.

## Что поддерживается сейчас
- Единый контракт ответа `OrchestratorResult` для всех обработчиков и инструментов.
- Маршрутизация: команды, smalltalk, summary, обычные вопросы.
- Локальные задачи: `echo`, `upper`, `json_pretty`.
- Меню на inline-кнопках (`/menu`) и wizard-сценарии календаря/напоминаний.
- Напоминания (список, snooze, перенос, отключение).
- Режим фактов (`/facts_on`, `/facts_off`) и контекст диалога.
- Веб-поиск `/search <запрос>` с ответом по источникам и списком источников внизу.

## Команды
- `/start`
- `/help`
- `/menu`
- `/ping`
- `/tasks`
- `/task <name> <payload>`
- `/profile` — показать профиль пользователя.
- `/profile_set` — мастер настройки профиля (язык, таймзона, подробность, напоминания).
- `/history` — последние действия (напоминания/календарь).
- `/history_find <запрос>` — поиск по истории действий.
- `/reminders [N]` (по умолчанию 10 ближайших)
- `/search <запрос>`
- `/search_sources list` — список источников поиска и их статус (вкл/выкл).
- `/search_sources enable <источник>` — включить источник.
- `/search_sources disable <источник>` — отключить источник.
- Обычный текст (маршрутизируется оркестратором).

Профиль пользователя влияет на ответы: язык, таймзона и подробность учитываются при формировании контекста.

## Документы (PDF/DOCX/изображения)
1. Отправьте боту файл в Telegram:
   - `document` с PDF/DOCX или изображение с текстом;
   - `photo` (будет обработан через OCR).
2. После обработки бот предложит действия:
   - **📝 Сделать резюме** — тезисное резюме на основе текста документа;
   - **❓ Вопрос по документу** — режим Q&A, ответы строятся только по тексту;
   - **🗑 Закрыть документ** — сбрасывает активную документ-сессию.
3. В Q&A доступны:
   - **🚪 Выйти из Q&A** — возвращает к выбору действий.

### Как работает Q&A
- Текст документа нарезается на чанки, выбираются релевантные фрагменты.
- В LLM передаются только эти фрагменты и вопрос.
- Если ответа в тексте нет — бот сообщает об этом, без фантазий.

### OCR
OCR для изображений использует `tesseract`. Если он не установлен, бот вернёт понятную ошибку.
Можно отключить OCR через `OCR_ENABLED=false` в `.env`.


## Управление источниками поиска
По умолчанию включены основной и резервный источник поиска (см. `config/orchestrator.json` → `search_sources`). Пользователь может отключать/включать источники:

- **`/search_sources list`** — показать все источники и статус (вкл/выкл) для текущего пользователя.
- **`/search_sources enable <id>`** — включить источник (например `perplexity` или `backup`).
- **`/search_sources disable <id>`** — отключить источник.

При поиске (`/search <запрос>`) используются только **включённые** источники в порядке приоритета. Если один источник недоступен или возвращает пустой результат, автоматически пробуется следующий (fallback). Состояние хранится в `data/search_sources.json` (путь можно задать через `SEARCH_SOURCES_STORE_PATH`).

## Напоминания (recurring + управление)
Создание через wizard “Напоминание”: после даты бот спросит повтор. Форматы:
- `none` — без повтора
- `daily` или `daily/2` (каждые 2 дня)
- `weekdays` или `weekdays/2` (по будням, каждые 2 недели)
- `weekly:1,3,5` или `weekly:1,3/2` (дни 0=Пн…6=Вс, каждые 2 недели)
- `monthly:15` или `monthly:15/2` (15-го числа, каждые 2 месяца)

При срабатывании напоминания доступны inline-кнопки:
**⏸ Отложить**, **✏ Перенести**, **🗑 Удалить**, **📋 Список**.

Напоминания могут использовать **контекст последнего диалога** (LLM-контекст): если при создании напоминания через wizard контекст диалога включён, его фрагмент сохраняется с напоминанием. При срабатывании бот передаёт контекст и текст напоминания в LLM и формирует более точное сообщение (например: «напомни через 2 часа про статью, которую я упоминал выше»).

## Result Contract
Поля `OrchestratorResult`:
- `text`, `status` (`ok/refused/error/ratelimited`), `mode` (`local/llm/tool`), `intent` (`namespace.action`), `request_id`
- `sources`, `attachments`, `actions`, `debug`

Правила:
- Любой handler/tool возвращает `OrchestratorResult`.
- Перед отправкой в UI применяется `ensure_valid`.

## Запуск

### Локально (venv)
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python bot.py
```

### Через Docker
Конфигурация только через переменные окружения (секреты не захардкожены в образ).

```bash
# Сборка
docker build -t secretary-bot:latest .
# или: make docker-build

# Запуск (передать BOT_TOKEN и ALLOWED_USER_IDS через -e или --env-file)
docker run --rm -it -e BOT_TOKEN=xxx -e ALLOWED_USER_IDS=123 secretary-bot:latest
# или с файлом: cp .env.example .env && заполнить .env → docker run --rm -it --env-file .env secretary-bot:latest
# или: make docker-run  (ожидает .env)
```

Данные (БД, календарь, allowlist) по умолчанию в `/app/data` внутри контейнера. Для сохранения между перезапусками смонтируйте том:
`docker run --rm -it -v bot-data:/app/data --env-file .env secretary-bot:latest`

### Docker Compose (локальный запуск)
Удобная обёртка без внешних БД:
```bash
cp .env.example .env
# заполнить BOT_TOKEN, ALLOWED_USER_IDS в .env
docker compose up --build
```
Том `bot-data` сохраняет данные между перезапусками.

## Переменные окружения
См. `.env.example` — в файле оставлены только актуальные переменные.

Обязательные для запуска (локально и в Docker): `BOT_TOKEN`, `ALLOWED_USER_IDS` (или allowlist в конфиге). Остальные — опционально с дефолтами (LLM: `OPENAI_API_KEY` или `PERPLEXITY_API_KEY`; CalDAV: `CALENDAR_BACKEND=caldav`, `CALDAV_URL`, `CALDAV_USERNAME`, `CALDAV_PASSWORD`; пути к данным, лимиты, флаги — см. `.env.example`).

### CalDAV (основной способ подключения календаря)
Добавьте в `.env`:
- `CALENDAR_BACKEND` (`local` или `caldav`, по умолчанию `local`)
- `CALDAV_URL` (URL календаря, например `https://nextcloud.example.com/remote.php/dav/calendars/user/default/`)
- `CALDAV_USERNAME`
- `CALDAV_PASSWORD` (app password)
- `CALDAV_CALENDAR_NAME` (необязательно; если не задан, берётся первый доступный для записи, предпочтение — `personal`)

## Подключение CalDAV
1. Создайте app password в вашем сервере (Nextcloud или совместимый).
2. Установите `CALENDAR_BACKEND=caldav` и заполните `CALDAV_URL`, `CALDAV_USERNAME`, `CALDAV_PASSWORD`.
3. В Telegram откройте **Menu → Settings → 📅 CalDAV → Подключить**, нажмите **Проверить подключение**.

### Manual smoke (Nextcloud)
1. В `.env` выставьте `CALENDAR_BACKEND=caldav` и заполните CalDAV-переменные.
2. Запустите бота и создайте событие через `/calendar add ...`.
3. Откройте Nextcloud Calendar и убедитесь, что событие появилось.

## Тесты
```bash
pytest
```
Локальный эквивалент CI: `make ci` (установка зависимостей + pytest).

## CI (GitHub Actions)
- **Триггеры:** push и pull_request в ветку `main`.
- **Шаги:** checkout → setup-python 3.11 с кэшем pip → установка зависимостей из `requirements.txt` → `pytest`; отдельный job собирает Docker-образ (`docker build .`), чтобы ловить поломки Dockerfile.
- **Статус:** вкладка *Actions* в репозитории GitHub; зелёный чек = тесты и сборка прошли.

## Поиск и строгий facts-mode
- `/search` без аргументов возвращает отказ с подсказкой: `Использование: /search <запрос>`.
- `/search <запрос>` выполняет веб-поиск, затем формирует ответ со сносками `[N]` и блоком `Источники:`.
- В режиме фактов (`/facts_on`) ответ допустим только при реальных `sources[]` и ссылках `[N]` внутри текста; если источники не найдены — `refused` без выдумок.
- Анти-псевдоцитаты: ссылки вида `[1]` и блок `Источники:` запрещены, если `sources[]` пустой.
