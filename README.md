# laughing-memory

Telegram-бот на архитектуре **Orchestrator v2**.

## Что поддерживается сейчас
- Единый контракт ответа `OrchestratorResult` для всех обработчиков и инструментов.
- Маршрутизация: команды, smalltalk, summary, обычные вопросы.
- Локальные задачи: `echo`, `upper`, `json_pretty`.
- Меню на inline-кнопках (`/menu`) и wizard-сценарии календаря/напоминаний.
- Напоминания (список, snooze, перенос, отключение).
- Режим фактов (`/facts_on`, `/facts_off`) и контекст диалога.
- Веб-поиск `/search <запрос>` с ответом по источникам и списком источников внизу.

## Команды
- `/start`
- `/help`
- `/menu`
- `/ping`
- `/tasks`
- `/task <name> <payload>`
- `/profile` — показать профиль пользователя.
- `/profile_set` — мастер настройки профиля (язык, таймзона, подробность, напоминания).
- `/history` — последние действия (напоминания/календарь).
- `/history_find <запрос>` — поиск по истории действий.
- `/reminders [N]` (по умолчанию 10 ближайших)
- `/search <запрос>`
- Обычный текст (маршрутизируется оркестратором).

Профиль пользователя влияет на ответы: язык, таймзона и подробность учитываются при формировании контекста.

## Документы (PDF/DOCX/изображения) — FileReader (Stage 6.5)
1. Отправьте боту файл в Telegram:
   - **document**: PDF или DOCX;
   - **photo** или **document** (изображение): текст извлекается через OCR (Tesseract).
2. После успешного извлечения текста бот ответит «Документ принят» и покажет кнопки:
   - **📝 Резюме** — краткое тезисное резюме (с LLM или эвристика без LLM);
   - **❓ Вопросы** — режим Q&A: последующие сообщения трактуются как вопросы по документу;
   - **🧹 Закрыть документ** — сброс сессии, возврат к обычному чату.
3. В режиме Q&A:
   - **🚪 Выйти из Q&A** — выход из режима вопросов;
   - **🧹 Закрыть документ** — закрыть документ.

Дополнительные команды: `/doc_close` — закрыть текущий документ; `/doc_status` — есть ли активный документ и сколько осталось до истечения сессии.

### Лимиты и конфиг (ENV)
| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `DOC_SESSION_TTL_SECONDS` | 7200 | Время жизни сессии документа (2 ч). По истечении бот просит прислать файл заново. |
| `FILE_MAX_BYTES_PDF_DOCX` | 10_000_000 | Макс. размер PDF/DOCX (10 МБ). |
| `FILE_MAX_BYTES_IMG` | 5_000_000 | Макс. размер изображения (5 МБ). |
| `DOC_MAX_CHARS` | 200_000 | Макс. символов извлечённого текста (обрезается с предупреждением). |
| `DOC_MAX_PAGES` | 50 | Макс. страниц PDF (лишние не обрабатываются). |
| `TESSERACT_LANG` | `rus+eng` | Языки для OCR (например `eng` или `rus+eng`). |
| `FILE_STORAGE_DIR` | `/tmp/laughing-memory-files` | Каталог для временных файлов (если используется). |
| `OCR_ENABLED` | `true` | Включить/выключить OCR для изображений. |

Файлы сохраняются во временное хранилище (диск: `UPLOADS_PATH`, `DOCUMENT_TEXTS_PATH`); сессии — в `DOCUMENT_SESSIONS_PATH`. После «Закрыть» или истечения TTL сессия удаляется; файлы на диске могут оставаться до очистки.

### Как работает Q&A
- Текст разбивается на чанки (≈800–1200 символов, overlap 150).
- По вопросу выбираются top-k релевантных чанков (простой скоринг по токенам).
- Если совпадений нет — ответ «В документе не нашёл ответа на этот вопрос».
- С LLM: в запрос передаются только выбранные фрагменты и строгая инструкция отвечать только по тексту.
- Без LLM: выводятся до 3 релевантных фрагментов как цитаты «Фрагмент документа».

### Резюме
- С LLM: краткое тезисное резюме (bullet list).
- Без LLM: первые абзацы + частотные ключевые слова (эвристика).

### OCR (Tesseract)
- Для распознавания текста на фото/скринах используется **Tesseract**. Язык задаётся через `TESSERACT_LANG` (по умолчанию `rus+eng`; при проблемах можно указать `eng`).
- Если Tesseract не установлен, бот вернёт сообщение вроде: «OCR недоступен. Установите tesseract или отключите OCR.» Можно отключить приём изображений по OCR через `OCR_ENABLED=false`.
- В Docker: для поддержки OCR добавьте в образ установку `tesseract-ocr` и нужных языковых пакетов (например `tesseract-ocr-rus`). Если OCR в образе не установлен, пайплайн не падает — при отправке изображения пользователь получит понятный отказ.


## Напоминания (recurring + управление)
Создание через wizard “Напоминание”: после даты бот спросит повтор. Форматы:
- `none` — без повтора
- `daily` или `daily/2` (каждые 2 дня)
- `weekdays` или `weekdays/2` (по будням, каждые 2 недели)
- `weekly:1,3,5` или `weekly:1,3/2` (дни 0=Пн…6=Вс, каждые 2 недели)
- `monthly:15` или `monthly:15/2` (15-го числа, каждые 2 месяца)

При срабатывании напоминания доступны inline-кнопки:
**⏸ Отложить**, **✏ Перенести**, **🗑 Удалить**, **📋 Список**.

## Result Contract
Поля `OrchestratorResult`:
- `text`, `status` (`ok/refused/error/ratelimited`), `mode` (`local/llm/tool`), `intent` (`namespace.action`), `request_id`
- `sources`, `attachments`, `actions`, `debug`

Правила:
- Любой handler/tool возвращает `OrchestratorResult`.
- Перед отправкой в UI применяется `ensure_valid`.

## Запуск

### Как сейчас (systemd на сервере)
На проде бот запускается через systemd (`telegram-bot.service`): `git pull` / `git reset` + перезапуск сервиса. Конфигурация юнита не меняется; переменные окружения задаются в unit или в `.env` в рабочей директории.

### Локально (venv)
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python bot.py
```

### Docker (воспроизводимость, не обязателен для прода)
- Сборка: `docker build -t telegram-bot .` (или `make docker-build`). Вариант с Python 3.11: `docker build --build-arg PYTHON_VERSION=3.11 -t telegram-bot .`
- Запуск: `docker run --rm -e BOT_TOKEN=... telegram-bot` (или `make docker-run` с выставленным `BOT_TOKEN`).
- Опционально для локального запуска: `docker compose up` (нужен `.env` или переменные окружения).

## CI (GitHub Actions)
При каждом `push` и `pull_request` запускается workflow: установка зависимостей (с кэшем pip), запуск pytest. Матрица: Python 3.11 и 3.12. Образы не публикуются — только тесты.

## Переменные окружения
См. `.env.example` — в файле оставлены только актуальные переменные.

### CalDAV (основной способ подключения календаря)
Добавьте в `.env`:
- `CALENDAR_BACKEND` (`local` или `caldav`, по умолчанию `local`)
- `CALDAV_URL` (URL календаря, например `https://nextcloud.example.com/remote.php/dav/calendars/user/default/`)
- `CALDAV_USERNAME`
- `CALDAV_PASSWORD` (app password)
- `CALDAV_CALENDAR_NAME` (необязательно; если не задан, берётся первый доступный для записи, предпочтение — `personal`)

## Подключение CalDAV
1. Создайте app password в вашем сервере (Nextcloud или совместимый).
2. Установите `CALENDAR_BACKEND=caldav` и заполните `CALDAV_URL`, `CALDAV_USERNAME`, `CALDAV_PASSWORD`.
3. В Telegram откройте **Menu → Settings → 📅 CalDAV → Подключить**, нажмите **Проверить подключение**.

### Manual smoke (Nextcloud)
1. В `.env` выставьте `CALENDAR_BACKEND=caldav` и заполните CalDAV-переменные.
2. Запустите бота и создайте событие через `/calendar add ...`.
3. Откройте Nextcloud Calendar и убедитесь, что событие появилось.

## Тесты
```bash
pytest
```

## Поиск и строгий facts-mode
- `/search` без аргументов возвращает отказ с подсказкой: `Использование: /search <запрос>`.
- `/search <запрос>` выполняет веб-поиск, затем формирует ответ со сносками `[N]` и блоком `Источники:`.
- В режиме фактов (`/facts_on`) ответ допустим только при реальных `sources[]` и ссылках `[N]` внутри текста; если источники не найдены — `refused` без выдумок.
- Анти-псевдоцитаты: ссылки вида `[1]` и блок `Источники:` запрещены, если `sources[]` пустой.
