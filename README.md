# laughing-memory

Telegram-бот на архитектуре **Orchestrator v2**.

## Что поддерживается сейчас
- Единый контракт ответа `OrchestratorResult` для всех обработчиков и инструментов.
- Маршрутизация: команды, smalltalk, summary, обычные вопросы.
- Локальные задачи: `echo`, `upper`, `json_pretty`.
- Меню на inline-кнопках (`/menu`) и wizard-сценарии календаря/напоминаний.
- Напоминания (список, snooze, перенос, отключение).
- Режим фактов (`/facts_on`, `/facts_off`) и контекст диалога.
- Веб-поиск `/search <запрос>` с ответом по источникам и списком источников внизу.

## Команды
- `/start`
- `/help`
- `/menu`
- `/ping`
- `/tasks`
- `/task <name> <payload>`
- `/reminders [N]`
- `/search <запрос>`
- Обычный текст (маршрутизируется оркестратором).


## Result Contract
Поля `OrchestratorResult`:
- `text`, `status`, `mode`, `intent`, `request_id`
- `sources`, `attachments`, `actions`, `debug`

Правила:
- Любой handler/tool возвращает `OrchestratorResult`.
- Перед отправкой в UI применяется `ensure_valid`.

## Запуск
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python bot.py
```

## Переменные окружения
См. `.env.example` — в файле оставлены только актуальные переменные.

### Calendar Backend
Бот поддерживает два типа календарей:
- **local** — локальное хранилище в JSON (по умолчанию)
- **caldav** — подключение к CalDAV-серверу (Nextcloud, iCloud, и др.)

Переменная `CALENDAR_BACKEND` определяет, какой бэкенд использовать:
- `CALENDAR_BACKEND=local` (по умолчанию) — события хранятся локально в `data/calendar.json`
- `CALENDAR_BACKEND=caldav` — события создаются через CalDAV API

### CalDAV (основной способ подключения календаря)
Для работы с CalDAV добавьте в `.env`:
- `CALENDAR_BACKEND=caldav`
- `CALDAV_URL` (URL календаря, например `https://nextcloud.example.com/remote.php/dav/calendars/user/default/`)
- `CALDAV_USERNAME`
- `CALDAV_PASSWORD` (app password — рекомендуется создать отдельный пароль приложения)
- `CALDAV_CALENDAR_NAME` (необязательно; если не задан, берётся первый доступный календарь)

**Важно:**
- При ошибках подключения к CalDAV бот автоматически использует локальное хранилище как fallback
- Пароли никогда не попадают в логи или debug-информацию
- События из CalDAV также сохраняются локально для работы с напоминаниями

### Google Calendar OAuth (опционально)
Добавьте в `.env`, если хотите оставить экспериментальную интеграцию:
- `GOOGLE_OAUTH_CLIENT_ID`
- `GOOGLE_OAUTH_CLIENT_SECRET`
- `PUBLIC_BASE_URL` (публичный базовый URL, например `https://your-domain` или `http://host:8080`)
- `GOOGLE_OAUTH_REDIRECT_PATH` (например `/oauth2/callback`)
- `GOOGLE_TOKENS_DB_PATH` (по умолчанию `data/google_tokens.db`)
- `GOOGLE_OAUTH_SERVER_PORT` (по умолчанию `8000`)

## Подключение CalDAV
1. **Создайте app password** в вашем CalDAV-сервере:
   - **Nextcloud**: Settings → Security → Devices & sessions → Create new app password
   - **iCloud**: appleid.apple.com → Security → App-Specific Passwords
   - Другие серверы: см. документацию вашего сервера

2. **Настройте переменные окружения** в `.env`:
   ```bash
   CALENDAR_BACKEND=caldav
   CALDAV_URL=https://nextcloud.example.com/remote.php/dav/calendars/username/personal/
   CALDAV_USERNAME=username
   CALDAV_PASSWORD=your-app-password
   CALDAV_CALENDAR_NAME=personal  # необязательно
   ```

3. **Перезапустите бота**:
   ```bash
   systemctl restart telegram-bot-web
   ```

4. **Проверьте подключение** (опционально):
   - В Telegram: **Menu → Settings → 📅 CalDAV → Подключить**
   - Нажмите **Проверить подключение**

### Примеры использования
После настройки CalDAV создавайте события как обычно:
```
/calendar add 2026-02-10 14:00 Встреча с командой
```

События будут созданы в вашем CalDAV календаре и появятся в Nextcloud/iCloud Calendar.

### Fallback на локальное хранилище
Если CalDAV недоступен (ошибка сети, неверные credentials), бот автоматически:
- Создаст событие локально
- Добавит в `debug`: `"calendar_backend": "local_fallback"`
- Залогирует ошибку (без паролей)

## Подключение Google Calendar (опционально)
1. В Google Cloud Console создайте OAuth Client (тип “Web application”).
2. В “Authorized redirect URIs” укажите:
   - `${PUBLIC_BASE_URL}${GOOGLE_OAUTH_REDIRECT_PATH}`  
     (например `https://your-domain/oauth2/callback`).
3. Заполните переменные окружения из секции выше.
4. Настройте reverse proxy на `/oauth2/` и `/health` → `http://127.0.0.1:8000`.
5. В Telegram откройте **Menu → Settings → 📅 CalDAV → Подключить**, пройдите авторизацию (если включали Google OAuth).

> Токены хранятся в SQLite (таблица `google_tokens`).  
> При деплое задайте права на файл БД `chmod 600`.

## Тесты
```bash
pytest
```

## Поиск и строгий facts-mode
- `/search` без аргументов возвращает отказ с подсказкой: `Использование: /search <запрос>`.
- `/search <запрос>` выполняет веб-поиск, затем формирует ответ со сносками `[N]` и блоком `Источники:`.
- В режиме фактов (`/facts_on`) ответ допустим только при реальных `sources[]`; если источники не найдены — `refused` без выдумок.
- Анти-псевдоцитаты: ссылки вида `[1]` и блок `Источники:` запрещены, если `sources[]` пустой.
