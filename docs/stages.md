# Список этапов (Stages)

- **Stage 4** — UX polish & human scenarios (menu, flows, fallbacks) — см. `.specify/specs/stage4-ux.md`
- **Stage 5** ✅ — Напоминания как ежедневный инструмент (recurring, inline-кнопки, daily digest) — **выполнен**
- **Stage 11** — источники/сноски в ответах (уже в тестах и коде)

## Stage 5 — что сделано

- Повторяемые напоминания (none/daily/weekdays/weekly/monthly + interval) в wizard создания.
- В списке напоминаний отображается признак повторяемости и следующий триггер.
- Inline-кнопки в уведомлении: ⏸ Отложить (snooze), ✏ Перенести (reschedule), 🗑 Удалить, 🏠 Меню.
- Все callback'и через безопасный формат (`cb:` или токен в ActionStore), лимит 64 байта, везде `answer_callback_query`.
- Snooze считается от `now`, не от исходного trigger_at; reschedule перепланирует job.
- Ежедневный дайджест: включение/выключение в Меню → Напоминания; раз в сутки (≈8:00 МСК) сводка на сегодня; пустой дайджест не отправляется.
- Документация в README обновлена.
